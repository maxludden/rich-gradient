name: CI

on:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, master ]

jobs:
  lint-test:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: [3.11, 3.12]
    steps:
      - uses: actions/checkout@v4
      - name: Setup Python
        uses: actions/setup-python@v4
        with:
          python-version: ${{ matrix.python-version }}
      - name: Install package
        run: |
          python -m pip install --upgrade pip
          pip install -e .
          pip install pytest mypy black isort ruff pre-commit
      - name: Run pre-commit
        run: pre-commit run --all-files || true
      - name: Run ruff
        run: ruff check .
      - name: Run mypy
        run: mypy src || true
      - name: Run tests
        run: pytest -q
